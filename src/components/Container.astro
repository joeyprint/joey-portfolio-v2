---
import { cn } from '@/lib/utils';

interface Props {
	className?: string;
	fluid?: boolean;
}

const { className } = Astro.props;
---

<div class={cn(`max-w-[1200px] mx-auto px-4`, className)}>
	<slot />
</div>
